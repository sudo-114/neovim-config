#!/bin/bash

# --- DEFAULT VARIABLES ---
PUBLIC_MODE=false
TARGET_DIR="."
DEFAULT_PORT=3000
TUNNEL_LOG="$HOME/.tunnel.log"

# ---- COLORS ----
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
MAGENTA='\033[0;35m'
CYAN='\033[0;36m'
BOLD='\033[1m'
DIM='\033[2m'
RESET='\033[0m'

# --- CLEANUP ---
cleanup() {
    # Kill all background jobs (the tunnel)
    kill $(jobs -p) 2>/dev/null || true
    rm -f "$TUNNEL_LOG"
}
trap cleanup EXIT

# --- LOGO ---
logo() {
    echo -e "${BOLD}${CYAN}"
    cat <<'EOF'
            ███████╗███████╗██████╗ ██╗   ██╗███████╗
            ██╔════╝██╔════╝██╔══██╗██║   ██║██╔════╝
            ███████╗█████╗  ██████╔╝██║   ██║█████╗  
            ╚════██║██╔══╝  ██╔══██╗╚██╗ ██╔╝██╔══╝  
            ███████║███████╗██║  ██║ ╚████╔╝ ███████╗
            ╚══════╝╚══════╝╚═╝  ╚═╝  ╚═══╝  ╚══════╝
EOF
    echo -e "${RESET}"
}

logo

# --- GLOBAL INSTALLER ---
# This allows 'serve --install' to work from anywhere
install_globally() {
    local target="$PREFIX/bin/serve"
    local source
    source=$(realpath "$0")

    echo -e "🔗 Linking ${BOLD}'$source'${RESET} to ${BOLD}'$target'${RESET}...\n"
    ln -sf "$source" "$target"
    chmod +x "$target"

    local runner='alias serve=". $PREFIX/bin/serve"'

    local shell=$(basename "$SHELL")

    if [ "$shell" = "bash" ]; then
        local shell_conf="bashrc"
    elif [ "$shell" = "zsh" ]; then
        local shell_conf="zshrc"
    else
        local shell_conf="bashrc" # fallback
    fi

    if [ ! -f ~/.$shell_conf ]; then
        touch ~/.$shell_conf
    fi

    if ! grep -q "$runner" ~/.$shell_conf; then
        echo "$runner" >>~/.$shell_conf
        echo -e "✅ Alias added to ${BOLD}~/.$shell_conf${RESET}\nRun '${BOLD}source ~/.$shell_conf${RESET}' or restart your shell to activate.\n"
    fi

    return 0 2>/dev/null || exit 0
}

# --- HELP MENU ---
show_help() {
    echo "SERVE v1.0"
    echo "Usage: serve [path] [options]"
    echo
    echo "Options:"
    echo "  [path]            Directory to serve (default: current)"
    echo "  -p, --port [num]  Specify port (default: 3000)"
    echo "  --public          Enable public URL & QR code"
    echo "  -i, --install     Link 'serve' to your system bin"
    echo "  -h, --help        Show this message"
}

# Check pkgs
check_dep() {
    local cmd_name="$1"
    local pkg_name="$1"

    if ! command -v "$cmd_name" &>/dev/null; then
        echo -e "⚠️Dependency missing: $cmd_name is not installed.\n"

        # Map command names to actual package names
        if [[ "$cmd_name" == "node" ]]; then
            pkg_name="nodejs"
        fi

        local install_cmd="sudo apt install $pkg_name -y"
        if [[ -n "$TERMUX_VERSION" ]]; then
            install_cmd="pkg install $pkg_name -y"
        fi

        echo "👉 Press [Enter] to run '$install_cmd' or [q] to quit: "
        read -r choice

        if [[ -z "$choice" ]]; then
            echo -e "\nRunning install...\n"
            eval "$install_cmd"
        else
            echo -e "${RED}❌ Cannot continue without $cmd_name. Exiting.${RESET}"
            exit 1
        fi
    fi
}

is_port_free() {
    (echo >/dev/tcp/127.0.0.1/$1) >/dev/null 2>&1 && return 1 || return 0
}

find_free_port() {
    local port="$1"
    local max=65535

    while ! is_port_free "$port"; do
        ((port++))
        if ((port > max)); then
            echo -e "${RED}❌ No free ports available${RESET}" >&2
            exit 1
        fi
    done

    echo "$port"
}

get_local_ip() {
    # Tries to find the LAN IP address
    ifconfig | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}' | head -n 1
}

PORT=$(find_free_port "$DEFAULT_PORT")

# A faster check that just installs if missing
silent_install() {
    if ! command -v "$1" &>/dev/null; then
        echo "🔧 Installing $1..."
        if [[ -n "$TERMUX_VERSION" ]]; then
            pkg install "$1" -y &>/dev/null
        else
            sudo apt install "$1" -y &>/dev/null
        fi
    fi
}

# --- PUBLIC TUNNEL ENGINE ---
start_public_tunnel() {
    # Ensure they have SSH and QR tools
    silent_install "ssh"

    if [[ -n $TERMUX_VERSION ]]; then
        silent_install "libqrencode"
    fi
    silent_install "qrencode"

    echo

    if [[ ! -f ~/.ssh/id_ed25519 ]]; then
        echo -e "🔑 No SSH key found. Generating..."
        mkdir -p ~/.ssh
        ssh-keygen -t ed25519 -N "" -f ~/.ssh/id_ed25519 <<<y >/dev/null 2>&1
    fi

    echo "🌐 Generating public link via localhost.run..."

    if [[ ! -f "$TUNNEL_LOG" ]]; then
        touch "$TUNNEL_LOG"
    fi

    # Start the tunnel in the background
    # We use a temp file to capture the assigned URL
    # We use -o StrictHostKeyChecking=no so it doesn't hang asking 'Are you sure?'
    ssh -n -R 80:localhost:"$PORT" -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ServerAliveCountMax=3 localhost.run >"$TUNNEL_LOG" 2>&1 &

    sleep 3
    PUBLIC_URL=$(grep -o 'https://[^ ]*\.lhr.life' "$TUNNEL_LOG" | head -n 1)

    echo
}

render_ui() {
    clear
    logo
    local local_ip=$(get_local_ip)

    echo -e "${BOLD}📂 Project:${RESET}  $PROJECT_TYPE"
    echo -e "${BOLD}📍 Local:${RESET}    http://localhost:$PORT"
    if [[ -n "$local_ip" ]]; then
        echo -e "${BOLD}🏠 Network:${RESET}  http://$local_ip:$PORT"
    fi

    if [[ "$PUBLIC_MODE" == "true" && -n "$PUBLIC_URL" ]]; then
        echo -e "${BOLD}🌍 Public:${RESET}   ${BLUE}$PUBLIC_URL${RESET}"
        echo
        if command -v qrencode &>/dev/null; then
            echo -e "${DIM}Scan for public access:${RESET}\n"
            qrencode -t ansiutf8 "$PUBLIC_URL"
        fi
    fi

    if [[ "$PUBLIC_MODE" == "true" && ! -n "$PUBLIC_URL" ]]; then
        echo -e "\n${RED}❌ Tunnel failed. Check your internet connection.${RESET}"
    fi

    echo -e "\n${DIM}Press Ctrl+C to stop${RESET}\n"
}

# --- THE ARGUMENT EATER ---
# This loop processes everything you type after the command 'serve'
while [[ "$#" -gt 0 ]]; do
    case "$1" in
    -i | --install)
        install_globally
        return 0 2>/dev/null || exit 0
        ;;
    --public) PUBLIC_MODE=true ;;
    -p | --port)
        PORT="$2"
        shift
        ;;
    -h | --help)
        show_help
        return 0 2>/dev/null || exit 0
        ;;
    *)
        if [ -d "$1" ]; then
            TARGET_DIR="$1"
        else
            echo -e "${RED}❌ Error: '$1' is not a valid option or directory.${RESET}"
            exit 1
        fi
        ;;
    esac
    shift
done

# Jump into the project folder before we start detecting files
# Clean up the path for the UI
DISPLAY_PATH="${TARGET_DIR/#$HOME/\~}"

# Professional spacing
echo
echo -e "📂 ${BOLD}Serving:${RESET} $DISPLAY_PATH"
echo "--------------------------------"

cd "$TARGET_DIR" || exit 1

# --- DETECTION ENGINE ---
# We do the work silently first
if [[ -f "package.json" ]]; then
    PROJECT_TYPE="node"

    [[ -f "pnpm-lock.yaml" ]] && MANAGER="pnpm" ||
        [[ -f "bun.lockb" ]] && MANAGER="bun" ||
        [[ -f "yarn.lock" ]] && MANAGER="yarn" || MANAGER="npm"

    # Then we print one clean line
    echo -e "🚀${GREEN} Node.js project detected: $MANAGER${RESET}"
    check_dep "node"

elif [[ -f "index.php" ]] || find . -maxdepth 1 -name "*.php" | grep -q .; then
    PROJECT_TYPE="php"

    echo -e "🐘 ${MAGENTA}PHP project detected${RESET}"
    check_dep "php"

else
    PROJECT_TYPE="static"

    echo -e "🌐 ${YELLOW}Static site detected${RESET}"
    check_dep "python3"
fi

# --- START TUNNELING ---
if [[ "$PUBLIC_MODE" == "true" ]]; then
    start_public_tunnel
fi

sleep 1
render_ui

# --- LAUNCH ENGINE ---
case "$PROJECT_TYPE" in
node)
    echo -e "⚡ ${DIM}Starting $MANAGER development server...${RESET}\n"
    # We use 'exec' so the server takes over the process
    $MANAGER run dev || $MANAGER start
    ;;

php)
    echo -e "⚡ ${DIM}Starting PHP built-in server on port $PORT...${RESET}\n"
    php -S 0.0.0.0:"$PORT"
    ;;

static)
    echo -e "⚡ ${DIM}Starting Python static server on port $PORT...${RESET}\n"
    python3 -m http.server "$PORT"
    ;;
esac
